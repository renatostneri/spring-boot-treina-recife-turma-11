spring:
  application:
    name: projeto
  datasource:
    url: jdbc:mysql://localhost/treinaSpring2
    username: root
    password: root
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        ddl-auto: validate

# Adicione estas linhas na raiz do arquivo:
logging:
  level:
    org.hibernate.orm.jdbc.bind: TRACE